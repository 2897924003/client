{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://test.opensun.asia/:path*"
    }
  ],
  "routes": [
  { "handle": "filesystem" },
  { "src": "/.*", "dest": "/" }
]
}
